from cloudmesh.common.debug import VERBOSE
import pprint

# What is the import statement for config?
from cloudmesh.configuration.Config import Config

# How do I print the config?

# config = Config()
# VERBOSE(config)
# print (config)
# pp = pprint.PrettyPrinter(indent=4)
# pp.pprint(config)

print ("Print statements were listed above\n Config was omitted since it has
passwords")
